/**
 * A read-only API for inspecting RAR archives and opening individual member
 * contents as streams.
 *
 * <p>This package never accepts an extraction destination. Archive member names
 * are metadata, not trusted host file-system paths.
 */
package io.github.ghosthack.unrar;
