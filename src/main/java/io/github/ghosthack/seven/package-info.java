/**
 * A small read-only facade over the 7z container format.
 *
 * <p>Archive member names are untrusted strings. This package never resolves
 * them against the host file system and has no archive-writing API.
 */
package io.github.ghosthack.seven;

